# WareCommand execution status

This tracker is the compact local checkpoint for Master Plan #108. Completed
rows mean the implementation and the applicable local evidence are committed;
they do not claim deployment or external-provider qualification. Each completed
row is also synchronized to its remote issue and #108 with the exact commit and
verification evidence.

| Issue | State | Acceptance evidence | Commit | Dependencies / blocker | Exact next action |
| ---: | --- | --- | --- | --- | --- |
| #2 | verified locally; committed | Restore passed; Debug and Release builds passed with 25 baseline warnings; 147/147 Release tests passed; MVC `/`, `/Dashboard`, `/Items`, `/Inventory` returned 200 against disposable SQLite; WinForms exposed a live main window; reproduction script passed | `f80c431` | Pre-existing untracked `Front-End/` prevents a clean working tree without destroying the supplied package | Upgrade the solution to .NET 10/C# 14 for #3 |
| #3 | verified locally; committed | .NET 10.0.401 SDK selected; all projects target .NET 10; stable package upgrades restored; Release build and 147/147 tests passed; MVC/WinForms baseline script passed | `342f1da` | Depends on #2; no production deployment or provider qualification claimed | Centralize package versions and establish strict quality gates for #4 |
| #4 | verified locally; committed | Central package management enabled; repository analyzer/editorconfig gates added; Release build passed with 0 warnings/0 errors; 147/147 Release tests passed; `dotnet format --verify-no-changes --severity error` passed; package graph reported no warnings/downgrades/conflicts; baseline script passed | `00d6700` | Depends on #3; performance analyzers CA1848/CA1873/CA1860 remain enabled as suggestions for targeted follow-up | Load and implement the acceptance requirements for #5 |
| #5 | verified locally; committed | Capability ownership documented in `ARCHITECTURE.md`; application/infrastructure registration extensions and Web/Desktop seed profiles centralize composition; Web/WinForms no longer construct persistence or demo domain data; 5 architecture tests plus 147 existing tests passed (152/152); format gate, package graph, and MVC/WinForms smoke script passed | `188429e` | Depends on #4; reserved capabilities remain explicit extension points and no production/provider readiness is claimed | Load and implement the acceptance requirements for #6 |
| #6 | verified locally; committed | Npgsql EF Core 10.0.3 is the production adapter; checked-in PostgreSQL migration covers identity generation, `numeric(18,4)` quantities, UTC audit timestamps, date-only lot fields, and non-distinct-null stock uniqueness; PostgreSQL 17 disposable integration passed with fresh migration, Web demo seed counts, Npgsql search translation, insert/query verification, and UTC readback; Release build passed with 0 warnings/0 errors; 152/153 solution tests passed with the provider test explicitly skipped when no test database is configured; format gate, package graph, MVC/WinForms baseline, Docker Compose config, and Dockerfile build passed | `5a1d606` | Depends on #5; production PostgreSQL requires an explicit connection string and SQLite remains explicit local/demo mode; no production migration, deployment, or push performed | Load and implement the acceptance requirements for #7 |
| #7 | verified locally; committed | Startup now refuses pending PostgreSQL migrations; explicit EF migration commands and idempotent SQL generation are documented and scripted; the read-only SQLite importer preserves identifiers, orders location parents, writes in one serializable transaction, resets identity sequences, creates a mandatory source backup, emits dry-run/apply reports, and reconciles row counts, stock totals, movement totals, and relationships; the real PostgreSQL 17 data-migration test passed apply/idempotency/rollback and source-hash checks; the tracked `Wms.ASP/warehouse.db` imported successfully into a disposable PostgreSQL target; Release build passed with 0 warnings/0 errors; 152 passed, 2 skipped, 154 total solution tests; format and migration lifecycle gates passed; CI wiring is checked in | `d80b4b7` | Depends on #6; no production migration, deployment, or push performed; the supplied untracked `Front-End/` package remains preserved | Load and implement the acceptance requirements for #8 |
| #8 | verified locally; committed | Tracked legacy `wms.db` and `warehouse.db` snapshots were copied outside the checkout and removed; `.gitignore` now covers runtime databases, logs, local secrets/settings, coverage, test results, and generated artifacts; one shared `WmsSeedService` provides idempotent `None`, minimal `Reference`, and opt-in `Demo` profiles for both hosts; production web defaults to schema-only seeding while Development/local desktop explicitly use Demo; 4 seed/profile tests passed; stale completion/test claims, unused `Form1`, and root validation utility were removed; README, architecture, testing, deployment, security, and roadmap entry points were added; Release build passed with 0 warnings/0 errors; 156 passed, 2 skipped, 158 total solution tests; format, migration lifecycle, MVC/WinForms smoke, PostgreSQL, and data-migration gates passed | `d717a1d` | Depends on #7; legacy WinForms folder naming is documented as deferred path churn; no production deployment or push performed; the supplied untracked `Front-End/` package remains preserved | Load and implement the acceptance requirements for #9 |
| #9 | verified locally; committed | Cross-platform GitHub Actions CI now restores on Linux and Windows, builds Linux-compatible projects and the complete WinForms solution, runs tests with coverage, checks formatting and migrations, checks NuGet vulnerabilities/deprecations, validates disposable PostgreSQL and SQLite-to-PostgreSQL gates, validates Compose, builds the production Docker image without pushing, scans for secrets, and reviews PR dependency changes; local dependency check passed with only the documented xUnit v2 legacy warning; Release build passed with 0 warnings/0 errors; 156 passed, 2 skipped, 158 total solution tests; format, migration, PowerShell parse, Compose, and Docker image gates passed; `gitleaks` was not installed locally, so the remote secret-scan job remains the authoritative execution | `73a31ab` | Depends on #8; the xUnit v2 legacy allowlist is temporary and must be removed after the test-framework migration; no push, deployment, or branch-protection mutation performed; the supplied untracked `Front-End/` package remains preserved | Load and implement #10; keep the issue tracker synchronized with each committed issue |
| #10 | verified locally; committed | Commit `9312a13` adds a .NET 10 multi-stage non-root production image with OCI version/revision metadata, runtime health probe, development/secrets exclusion, persistent Data Protection storage, configurable forwarded headers, graceful shutdown timeout, and explicit secret-file PostgreSQL connection resolution; `docker-compose.yml` provides Web plus PostgreSQL with migration-gated startup, internal backend/edge networks, named database/key volumes, restart policies, loopback bindings, and real liveness/readiness checks; production configuration and reverse-proxy/HTTPS/logging/secret/restart guidance are documented; local no-cache Docker build passed, image ran as UID 1654, development/example config and source were absent from the runtime image, both Compose configs parsed, explicit PostgreSQL migration passed, `/health/live` and `/health/ready` returned 200, readiness returned 503 during a database outage and 200 after recovery, Data Protection keys survived Web restart, graceful stop exited 0, and exact validation containers/volumes were removed; Release build passed with 0 warnings/0 errors; 156 passed, 2 skipped, 158 total solution tests; format, dependency, migration, and MVC/WinForms smoke gates passed | `9312a13` | Depends on #3, #6, and #7; no production deployment, live migration, or push performed; the supplied untracked `Front-End/` package remains preserved | Load and implement #11; keep the issue tracker synchronized with each committed issue |
| #11 | verified locally; committed | Commits `796de67` and `acdc15e` add `CONTRIBUTING.md` with lightweight branch/PR rules, Conventional Commits, SemVer/pre-release/tag policy, migration/API compatibility, support policy, changelog process, issue-closure evidence, no-auto-push policy, and a dry-run release/tag plan; add `CHANGELOG.md`, pull-request template, feature/bug/migration/release issue templates, and `docs/release/RELEASE_CHECKLIST.md` covering backups, restore, migrations, compatibility, health/smoke, monitoring, Data Protection, rollout, and rollback; the generic release-directory ignore was narrowed so the checklist is tracked; governance/front matter/sample `v0.1.0-rc.1` tag format validated without creating a tag; restore passed; Release build passed with 0 warnings/0 errors; 156 passed, 2 skipped, 158 total solution tests; format and migration lifecycle gates passed | `796de67`, `acdc15e` | Depends on #3 and #10; no release tag, push, deployment, or production migration created; the supplied untracked `Front-End/` package remains preserved | Load and implement #12; keep the issue tracker synchronized with each committed issue |
| #12 | verified locally; committed | First-party ASP.NET Core Identity user/role schema and `WmsAuthenticationEvents` migration; admin-created accounts with explicit secret-only bootstrap; password policy, lockout, disabled-account enforcement, secure sliding cookies, antiforgery login/logout/reset/change-password flows, account-management screens, SMTP reset adapter, and persistent Data Protection configuration; MVC warehouse routes now require `ICurrentUser` and persist real Identity IDs; WinForms uses an explicit login dialog/session and no `SYSTEM` fallback; 4 focused Infrastructure Identity tests passed plus 5 MVC integration tests; full baseline passed with 165 passed and 2 provider-gated skips; Release build passed with 0 warnings/0 errors; migration lifecycle, formatter, disposable PostgreSQL integration, Compose config, and authenticated MVC/WinForms smoke gates passed | `1f0b49e` | Depends on #3, #6, #7, and #10; bootstrap is disabled by default; SMTP and production secret/provider rollout remain environment-scoped; no production deployment or push performed; the supplied untracked `Front-End/` package remains preserved | Load and implement #13; keep the issue tracker synchronized with each committed issue |
| #13 | verified locally; committed | Permission-based RBAC for Administrator, Warehouse Manager, Receiver, Picker, Packer, Inventory Controller, Auditor, Viewer, and the controlled WarehouseStaff bootstrap role; seeded permission matrix and policy handler; persisted user-to-warehouse/default assignments; server-side use-case authorization; scoped location, stock, movement, and warehouse option queries; cross-warehouse ID rejection; security warnings for denied authorization; administrator access-management screen with last-admin/self-lockout safeguards; migration `20260920000339_AddWarehouseAuthorization`; authorization integration tests cover denied routes, changed permissions, assigned warehouse visibility, and cross-warehouse movement filtering; Release build 0 warnings/0 errors; 171 passed, 2 provider-gated skips, 173 total solution tests; format, migration lifecycle, and whitespace gates passed | `16b6aba` | Depends on #12 authentication/current-user identity; no production migration, deployment, or push performed; the supplied untracked `Front-End/` package remains preserved; broad immutable audit coverage is now #14 | Load and implement #14; keep the issue tracker synchronized with each committed issue |
| #14 | verified locally; committed | `IAuditWriter` with `IClock`, request/correlation, source-client, and warehouse context contracts; append-only `WmsAuditEntries` model and PostgreSQL migration `20260920004831_AddImmutableAuditLogging`; `WmsDbContext` rejects normal audit updates/deletes; bounded metadata redaction covers credentials, tokens, connection strings, security stamps, and sensitive personal-data keys; account creation/status, authentication, access changes, item/location master data, receipts, putaway, picks, and stock adjustments are recorded at their current mutation boundaries; MVC request context and Desktop source wiring; `audit.read` RBAC, warehouse-scoped paginated audit screen, filterable CSV export capped at 10,000 rows, and retention/export/database-role policy in `docs/modernization/AUDITING.md`; 18 focused audit/authorization/stock tests plus 9 ASP tests passed; Release build 0 warnings/0 errors; 177 Release tests passed with 2 provider-gated skips; format and migration lifecycle gates passed | `b0be85a` | Depends on #12/#13; PostgreSQL provider tests were explicitly skipped because no test database was configured; production database role grants, archive storage, scheduled retention, deployment, and push were not performed; transfer/count/allocation/pack/shipment/return/settings/integration workflows are not present in the current model and have reserved audit actions for their owning issues | Load and implement #15; use the shared audit contract for each new workflow and keep the issue tracker synchronized |
| #15 | verified locally; committed | Global MVC antiforgery validation; nonce-based CSP and secure response headers; account no-store caching; fail-closed trusted forwarded headers with HSTS after proxy processing; Kestrel/form request limits; named authentication, password-reset, report, API, scanning, and import rate-limit policies; server-side validation and controller bind allowlists; open-redirect, overposting, path-traversal, XSS/SQL, secret-storage, and log-redaction review; updated deployment/security guidance and focused CSRF/header/rate-limit tests; Release build 0 warnings/0 errors; 181 Release tests passed with 2 provider-gated skips; format, migration lifecycle, Compose, JSON configuration, and whitespace gates passed | `79483cd` | Depends on #12/#13/#14; the current host has no upload, scanning, import, or API endpoints, so those policies are prepared for the owning endpoints; proxy values, secrets, production provider qualification, deployment, and push remain environment-scoped | Load and implement #17; preserve the security defaults and keep the issue tracker synchronized |
| #16 | verified locally; committed | Implementation `55d6bc9` standardizes typed `ResultError`/`WmsErrors` categories, stable codes, safe messages, field errors, and retryability while preserving the existing Result compatibility surface; adds the global scoped `WmsExceptionHandler` with safe RFC-style API Problem Details and friendly MVC error handling; maps validation, authorization, not-found, unique-key, concurrency, dependency, cancellation, and unexpected failures without exposing provider details or internal identifiers; preserves correlation IDs and error references in response/logging scopes; propagates `CancellationToken`, rethrows cancellation, removes broad controller error boilerplate, maps expected failures to UI ModelState, and adds focused exception/result/cancellation coverage; procedure documented in `docs/modernization/ERROR_HANDLING.md`; Release build 0 warnings/0 errors; 188 Release tests passed with 2 provider-gated skips; format, migration lifecycle, and whitespace gates passed | `55d6bc9` | Depends on #14 request/correlation context and #17 structured Serilog/correlation standards for fuller operational coverage; the current host has no API or background-job endpoints, so endpoint/job-specific adapters remain with their owning issues; no provider/live qualification, production deployment, migration, or push performed | Load and implement #18; preserve the typed error and cancellation contracts and keep the issue tracker synchronized |
| #17 | verified locally; committed | Implementation `b97f9e0` configures shared Serilog JSON logging in MVC and WinForms with configurable minimum levels, framework overrides, console output, bounded rolling files, environment enrichment, and startup/shutdown lifecycle events; adds the async `WmsOperationContext` contract/accessor and `X-Correlation-ID`/`X-Operation-ID`/`X-Reference-ID` propagation for web and future job/API/integration boundaries; enriches requests and inventory operations with correlation, operation/reference, request, client, user, and warehouse properties; defines stable event IDs for requests, slow operations, migrations, inventory mutations, jobs, external calls, and authentication; logs migration and critical receipt/putaway/pick/adjustment outcomes; removes raw barcode/debug logging; safe JSON formatting redacts credentials, tokens, connection strings, barcode/payload/body and selected personal-data properties, bounds large values, and omits stack traces; procedure documented in `docs/modernization/LOGGING.md`; Release build 0 warnings/0 errors; 194 Release tests passed with 2 provider-gated skips; format, migration lifecycle, JSON configuration, and whitespace gates passed | `b97f9e0` | Depends on #12/#13/#14 current-user, warehouse, audit, and request context; the current host has no durable jobs, public API, or external integration adapters, so their concrete propagation and start/completion/failure wiring remain with #21/#88–#93; no provider/live qualification, production deployment, migration, or push performed | Continue with #19; preserve the logging/redaction and operation-context contracts and keep the issue tracker synchronized |
| #18 | verified locally; committed | Commit `32cbce1` adds OpenTelemetry 1.19 tracing/metrics for ASP.NET Core, HttpClient, EF Core, runtime, and the shared WMS activity source; bounded request/database/inventory/conflict/job/backlog/storage/backup instruments; custom inventory and future external-operation activities without payload/SQL/secret tags; separate anonymous terse `/health/live` and `/health/ready` probes with database reachability/pending-migration, storage/free-space, critical-configuration, startup, and fail-closed job-runner checks; OTLP/console exporters, standard `OTEL_*` settings, sampling, Docker configuration, alert conditions, and runbook documentation; 3 focused ASP observability tests plus 2 focused telemetry tests; disposable SQLite health outage/recovery passed; console exporter emitted a real HTTP trace and custom metrics; Release build 0 warnings/0 errors; 200 Release tests passed with 2 provider-gated skips; format, migration, dependency, JSON, and whitespace gates passed | `32cbce1` | Depends on #17, PostgreSQL, and #21 job architecture; the current host has no durable job runner, pack/ship workflows, external adapters, or backup executor, so their concrete health/throughput producers remain with #21/#51–#59/#88–#93 and the PostgreSQL integration suite remains explicitly skipped without a configured test database; no production deployment, live collector/provider qualification, migration, or push performed | Load and implement #19; preserve telemetry contracts and keep the issue tracker synchronized |
| #19 | verified locally; committed | Typed explicit settings for company, warehouse defaults, numbering, dashboard thresholds, inventory/expiry, scanner, labels, reports, localization, and integrations; global values plus nullable warehouse overrides with deterministic precedence, defaults, validation, revision checks, cache invalidation, relational location/warehouse safety, nonsecret import/export, and restart/live-reload guidance; Administrator/warehouse-scoped settings UI with allowlisted forms, antiforgery, import rate limiting, authorization, and shared audit writes; Dashboard, receiving, putaway, location creation, and desktop dashboard now consume typed settings instead of operational magic values; migrations `20260920035416_AddTypedBusinessSettings` and `20260920040056_AddDashboardMovementLimitSetting`; Release build 0 warnings/0 errors; 208 Release tests passed with 2 provider-gated skips; focused settings tests passed (4 application, 1 persistence, 1 cache, 2 ASP); format, migration lifecycle, JSON configuration, and whitespace gates passed | `0838db9` | Depends on #12/#13/#14; no production migration, deployment, push, secret rollout, or live PostgreSQL qualification performed; external provider integration values remain deployment configuration and future workflows still need to consume their relevant settings | Load and implement #20; preserve typed settings, authorization, audit, cache, and secret-boundary contracts |
| #20 | verified locally; committed | Commit `79c32b7` adds the shared English/Arabic resource catalog with 473-key parity, supported `en-US`/`ar-SA` culture catalog, query/cookie/user/global-settings culture precedence, localized MVC validation/errors/navigation/views, RTL/LTR Bootstrap and Cairo presentation, technical identifier direction isolation, localized dates/numbers/quantities, account language preference persistence, and WinForms localization with session culture persistence; request-level Arabic RTL and English LTR flows plus anonymous locale-cookie persistence are covered by `LocalizationFlowTests`; resource parity/directionality coverage is in `WmsLocalizationResourceTests`; the typed settings test was updated to the canonical localized wording; `docs/modernization/LOCALIZATION.md` documents the contract; Release build passed with 0 warnings/0 errors; 212 Release tests passed with 2 provider-gated skips; focused localization tests passed 4/4; format and whitespace gates passed | `79c32b7` | Depends on #19 typed localization settings; no production deployment, browser screenshot/Playwright run, live provider qualification, migration, or push performed; browser/device/font rendering and provider-specific qualification remain environment-scoped | Load and implement #21; preserve culture precedence, resource parity, RTL/LTR identifier isolation, and tracker synchronization |
| #21 | verified locally; committed | Commit `1d1773e` adds the PostgreSQL-backed Hangfire runner and checked-in `20260920053427_AddDurableBackgroundJobs` migration with `WmsJobExecutions` idempotency/attempt ledger and deduplicated `WmsJobNotifications`; registers stable recurring definitions across `default`, `reports`, `integrations`, `alerts`, and `maintenance` queues; preserves correlation, actor, warehouse, timeout, cancellation, bounded retry, permanent-failure/dead-letter, notification, health/backlog telemetry, graceful-shutdown, and admin-only `/jobs` dashboard boundaries; implements expiry, low-stock, scheduled movement-report, cleanup, and explicit future-domain adapters without moving stock commits into detached work; adds catalog/store tests; Release solution build passed with 0 warnings/0 errors; 218 Release tests passed with 2 provider-gated PostgreSQL skips; `dotnet format --verify-no-changes --severity error`, EF model-drift check, JSON, Compose, and whitespace gates passed | `1d1773e` | Depends on #12/#13 auth/RBAC, #14 audit context, #17 logging, #18 telemetry, and #19 typed settings; Hangfire/PostgreSQL live restart/retry qualification is provider-gated because no `WARECOMMAND_TEST_POSTGRES_CONNECTION` is configured; jobs remain disabled by default; no production migration, deployment, or push performed; integration outbox, cycle-count policy, replenishment policy, and browser/device qualification remain with their owning issues | Load and implement #22; preserve the durable idempotency, timeout/cancellation, queue, actor/correlation, retry, and admin-dashboard contracts; keep #21/#108 synchronized |
| #22–#107 | pending | Not yet evaluated as complete | — | Ordered by the master plan and prerequisite constraints; #22 is the next dependency-ordered issue | Implement #22 next; record blocked/dependent scope and remote progress in this tracker and #108 |

## Phase checkpoint

- Phase 0: #2 through #11 committed.
- Phase 1: #12 through #21 committed; #22 through #23 and #81 through #84 remain pending.
- Phase 2–9: pending implementation and qualification.
- Nothing has been pushed, deployed, or migrated against production data.

## Evidence paths

- Baseline narrative: `docs/modernization/BASELINE_STATUS.md`
- Reproduction command: `scripts/verify-baseline.ps1`
- Approved frontend package: pre-existing untracked `Front-End/WareCommand-Landing-Page/`
- Authentication qualification: `Wms.Infrastructure.Tests` Identity tests and `Wms.ASP.Tests` MVC host tests
- Authentication procedure: `docs/modernization/AUTHENTICATION.md`
- Authorization qualification: `Wms.Infrastructure.Tests/Identity/WmsAuthorizationTests.cs` and `Wms.ASP.Tests/AuthorizationFlowTests.cs`
- Authorization procedure: `docs/modernization/AUTHORIZATION.md`
- Audit qualification: `Wms.Infrastructure.Tests/Auditing/AuditEntryTests.cs`, `Wms.Infrastructure.Tests/Services/StockMovementServiceTests.cs`, and `Wms.ASP.Tests/AuthorizationFlowTests.cs`
- Audit procedure and retention/export policy: `docs/modernization/AUDITING.md`
- Web security qualification: `Wms.ASP.Tests/AuthenticationFlowTests.cs`, `Wms.ASP/Security/WmsSecurityOptions.cs`, `SECURITY.md`, and `DEPLOYMENT.md`
- Error-handling qualification: `Wms.ASP/Errors/WmsExceptionHandling.cs`, `Wms.Application/Common/ResultError.cs`, `Wms.ASP.Tests/AuthenticationFlowTests.cs`, and `Wms.Application.Tests/ResultTests.cs`
- Error-handling procedure: `docs/modernization/ERROR_HANDLING.md`
- Logging qualification: `Wms.Infrastructure.Tests/Logging/WmsLoggingTests.cs`, `Wms.ASP.Tests/AuthenticationFlowTests.cs`, and `Wms.Application/Logging/WmsLogEvents.cs`
- Logging procedure: `docs/modernization/LOGGING.md`
- Observability qualification: `Wms.ASP.Tests/ObservabilityFlowTests.cs`, `Wms.ASP.Tests/Health/WmsDatabaseHealthCheckTests.cs`, and `Wms.Infrastructure.Tests/Telemetry/WmsTelemetryTests.cs`
- Observability procedure and alert/runbook guidance: `docs/modernization/OBSERVABILITY.md` and `docs/operations/OBSERVABILITY_RUNBOOK.md`
- Durable job qualification: `Wms.Application.Tests/Jobs/WmsJobCatalogTests.cs`, `Wms.Infrastructure.Tests/Jobs/WmsJobExecutionStoreTests.cs`, `Wms.Infrastructure/Database/Migrations/20260920053427_AddDurableBackgroundJobs.cs`, and `docs/modernization/JOBS.md`
- Settings qualification: `Wms.Application.Tests/Settings/WmsSettingsTests.cs`, `Wms.Infrastructure.Tests/Settings/WmsSettingsPersistenceTests.cs`, `Wms.Infrastructure.Tests/Settings/WmsSettingsCacheTests.cs`, and `Wms.ASP.Tests/SettingsFlowTests.cs`
- Settings procedure and schema: `docs/modernization/SETTINGS.md`, `Wms.Infrastructure/Database/Migrations/20260920035416_AddTypedBusinessSettings.cs`, and `Wms.Infrastructure/Database/Migrations/20260920040056_AddDashboardMovementLimitSetting.cs`
