@{
    ViewData["Title"] = "Reset link requested";
}

<div class="alert alert-info">
    If an active account matches that email address, a password reset link has been sent. The link expires according to the configured Identity token policy.
</div>
<a asp-action="Login" class="btn btn-primary">Return to sign in</a>
